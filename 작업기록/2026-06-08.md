# 2026-06-08 작업기록

## Sprint 2.3 - 카메라 안정화 및 관전 품질 개선

### 작업 내용

- 카메라 추적 방식을 선두 1명 추적에서 TOP5 평균 위치 추적으로 변경했다.
- Dead Zone을 `120px`로 재도입했다.
- 카메라 목표 위치에 `0.14` 스무딩을 적용했다.
- 카메라 이동량을 프레임당 최대 `24px`로 제한했다.
- 카메라 follow 값을 `0.18`로 재조정했다.
- 테스트 조건에 맞춰 최대 공 수 제한을 `100`으로 조정했다.
- 물리값은 변경하지 않았다.
- 헤더 버전을 `Sprint 2.3`으로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 카메라 값

- 추적 방식: `TOP5 average`
- deadzone: `120`
- camera follow: `0.18`
- target smoothing: `0.14`
- max step: `24`
- view anchor: `0.48`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 `200` 확인
- 서버 화면에서 `Sprint 2.3` 반영 확인
- 15개 공 시뮬레이션: 화면 이탈 `0`, 점프 `0`, 최대 이동 `3.25px/frame`
- 60개 공 시뮬레이션: 화면 이탈 `0`, 점프 `0`, 최대 이동 `3.42px/frame`
- 100개 공 시뮬레이션: 화면 이탈 `0`, 점프 `0`, 최대 이동 `3.42px/frame`

### 남은 문제점

- 실제 브라우저 관전 기준 미세 떨림 체감은 추가 확인이 필요하다.
- 100개 공 실제 플레이에서 이름 표시 밀도와 성능 확인이 필요하다.

## Sprint 2.4 - 물리 신뢰성 확보 및 쇠구슬 감성 구현

### 작업 내용

- CAMERA ON / OFF 테스트 모드를 추가했다.
- 공 상태에 `prevX`, `prevY`, `angle`, `angularVelocity`를 추가했다.
- 이전 위치와 현재 위치 사이를 3회 샘플링하는 swept collision 검사를 추가했다.
- 막대형 장애물, 회전 바, 해머, 범퍼 충돌에 swept 검사를 적용했다.
- collision solver iteration을 `2`로 설정했다.
- 공 표면에 회전 마커와 하이라이트를 추가했다.
- 회전 속도와 굴림 저항을 적용했다.
- HUD에 선두 공 속도 `v`와 각속도 `av`를 표시했다.
- 헤더 버전을 `Sprint 2.4`로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 주요 값

- gravity: `0.066`
- friction: `0.982`
- damping: `0.994`
- restitution: `0.52`
- maxFallSpeed: `5.1`
- solver iterations: `2`
- CCD samples: `3`
- rolling resistance: `0.992`
- angular damping: `0.965`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 `200` 확인
- 서버 화면에서 `Sprint 2.4` 반영 확인

### 남은 문제점

- CAMERA ON / OFF 비교를 실제 브라우저에서 육안으로 진행해야 떨림 원인을 확정할 수 있다.
- 장애물 관통 현상은 swept collision으로 줄였지만 실제 고속 충돌 상황에서 추가 확인이 필요하다.

## Sprint 2.45 - 템포 회복 및 굴림감 개선

### 작업 내용

- gravity를 `0.066`에서 `0.076`으로 조정했다.
- damping을 `0.994`에서 `0.997`로 조정했다.
- rolling resistance를 `0.992`에서 `0.996`으로 조정했다.
- 가이드 바 접촉 시 속도 보존과 하강 보조를 소폭 강화했다.
- 회전 반응 계수를 `0.18`에서 `0.24`로 올렸다.
- 공 회전 마커 크기와 대비를 강화했다.
- 기존 swept collision, solver iteration, CCD samples는 유지했다.
- 후미 공 정체 방지를 위해 자동 탈출 기준을 `2400ms`에서 `2000ms`로 줄였다.
- 36초 이후 후미 공 템포 가드를 적용하고, 45초 이후 추가 진행 보조를 적용했다.
- 헤더 버전을 `Sprint 2.45`로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 주요 값

- gravity: `0.076`
- damping: `0.997`
- rolling resistance: `0.996`
- friction: `0.982`
- restitution: `0.52`
- maxFallSpeed: `5.1`
- solver iterations: `2`
- CCD samples: `3`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 `200` 확인
- 서버 화면에서 `Sprint 2.45` 반영 확인
- 물리 시뮬레이션 평균 첫 도착: 약 `27.0초`
- 물리 시뮬레이션 평균 전체 종료: 약 `40.4초`

### 남은 문제점

- 실제 브라우저 기준 CAMERA ON / OFF 체감 테스트가 필요하다.
- 장애물 관통 재발 여부는 실제 고속 충돌 상황에서 추가 확인해야 한다.

## Sprint 2.5 - 쇠구슬 질량감 완성 및 동적 충돌 안정화

### 작업 내용

- gravity를 `0.076`에서 `0.080`으로 조정했다.
- damping을 `0.997`에서 `0.998`로 조정했다.
- rolling resistance를 `0.996`에서 `0.997`로 조정했다.
- 회전 바에 moving swept collision을 적용했다.
- 해머에 moving swept collision을 적용했다.
- 이동 플랫폼에 moving swept collision을 적용했다.
- 동적 장애물 충돌 시 회전량과 충돌 에너지 전달을 강화했다.
- 회전 마커 크기와 대비를 강화했다.
- 헤더 버전을 `Sprint 2.5`로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 주요 값

- gravity: `0.080`
- damping: `0.998`
- rolling resistance: `0.997`
- friction: `0.982`
- restitution: `0.52`
- maxFallSpeed: `5.1`
- solver iterations: `2`
- CCD samples: `3`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 확인
- 서버 화면에서 `Sprint 2.5` 반영 확인
- 동적 장애물 CCD 스트레스 시뮬레이션에서 회전 바, 해머, 이동 플랫폼 접촉 감지 확인

### 남은 문제점

- 실제 브라우저 기준 평균 경기 시간은 추가 계측이 필요하다.
- 동적 장애물 관통이 실제 플레이에서 재발하지 않는지 육안 확인이 필요하다.

## Sprint 2.6 - 쇠구슬 질량감 완성

### 작업 내용

- gravity는 `0.080`으로 유지해 하강 에너지를 낮추지 않았다.
- damping을 `0.998`에서 `0.994`로 낮춰 공기 저항처럼 보이는 감속을 줄였다.
- rolling resistance를 `0.997`에서 `0.992`로 낮춰 굴림 에너지 유지감을 강화했다.
- 범퍼, 가이드 바, 스피너, 해머, 이동 플랫폼 충돌 시 속도와 각속도 전달을 강화했다.
- 충돌 후 공이 바로 죽지 않고 진행 에너지를 더 유지하도록 보정했다.
- 회전 마커 크기와 대비를 강화해 굴러가는 느낌을 더 잘 보이게 했다.
- Sprint 2.5의 moving swept collision, dynamic CCD, solver iteration, CCD samples, CAMERA ON/OFF 테스트 모드는 유지했다.
- 헤더 버전을 `Sprint 2.6`으로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 주요 값

- gravity: `0.080`
- damping: `0.994`
- rolling resistance: `0.992`
- friction: `0.982`
- restitution: `0.52`
- maxFallSpeed: `5.1`
- solver iterations: `2`
- CCD samples: `3`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 확인
- 화면 버전 `Sprint 2.6` 반영
- 코드 기준 동적 장애물 moving swept collision 유지 확인

### 남은 문제점

- 실제 브라우저 기준 평균 경기 시간은 추가 계측이 필요하다.
- 동적 장애물 관통이 실제 플레이에서 재발하지 않는지 육안 확인이 필요하다.
- CAMERA ON 상태에서 질량감과 카메라 안정성이 함께 유지되는지 실제 관전 테스트가 필요하다.

## Sprint 2.7 - 다중 루트 레이스 맵 구조 개편

### 작업 내용

- 초반 분기를 `3갈래 루트`로 길게 확장해 시작 직후 경로 선택이 발생하도록 했다.
- 초반 루트가 중앙으로 다시 모이도록 가이드 바와 범퍼를 재배치했다.
- 중반에 긴 S자 레이스 구간을 배치해 직선 낙하 느낌을 줄였다.
- 후반에 고속/안정/변수 성격의 재분기 구간을 배치했다.
- 결승 전 모든 루트가 다시 모이는 혼전 구간을 만들었다.
- 회전 장애물을 구간별로 띄워 배치해 과밀도를 줄였다.
- 빈 낙하 구간을 줄이기 위해 범퍼, 가이드 바, C자 함정, 점프 구간을 균등하게 재배치했다.
- Sprint 2.6 물리값은 유지했다.
- 헤더 버전을 `Sprint 2.7`로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 맵 구성

- 분기 수: `3`
- 합류 지점 수: `2`
- 범퍼: `15`
- 가이드 바: `26`
- 스피너: `4`
- 해머: `3`
- 이동 플랫폼: `2`
- 점프 구간: `3`
- C자 함정: `4`
- S자 통로: `2`
- 동적 장애물 수: `9`

### 유지한 물리값

- gravity: `0.080`
- damping: `0.994`
- rolling resistance: `0.992`
- friction: `0.982`
- restitution: `0.52`
- maxFallSpeed: `5.1`
- solver iterations: `2`
- CCD samples: `3`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 확인
- 화면 버전 `Sprint 2.7` 반영 확인
- 물리값 유지 확인

### 남은 문제점

- 실제 브라우저 기준 평균 경기 시간은 추가 계측이 필요하다.
- 실제 플레이 기준 순위 변동 횟수는 추가 계측이 필요하다.
- 결승 혼전 구간에서 실제로 TOP 참가자들이 충분히 다시 만나는지 육안 확인이 필요하다.
- 일부 기존 섹션 라벨 문자열은 추후 별도 UI 정리 작업에서 개선이 필요하다.

## Sprint 2.8 - 레이스 트랙 구조 강제 적용

### 작업 내용

- 화면에서 바로 3개 루트를 인식할 수 있도록 `TRACK_ZONES` 시각 레이어를 추가했다.
- 초반 A / B / C 루트를 맵 전체 폭의 약 80%까지 사용하도록 확장했다.
- 후반 FAST / SAFE / CHAOS 루트도 동일한 폭으로 명확히 분리했다.
- 중앙 합류 구간을 약 300px 폭으로 압축해 공들이 실제로 섞이도록 유도했다.
- 결승 병목 구간을 약 196px 폭으로 표시해 난전이 발생하는 구조를 만들었다.
- 6개 섹션 기준으로 주요 장애물 밀도를 2~4개 수준으로 정리했다.
- 동적 장애물은 섹션당 최대 1개가 되도록 `9개`에서 `6개`로 줄였다.
- 물리값과 카메라값은 변경하지 않았다.
- 물리 반경은 유지하고 시각 공 크기만 `36px`에서 `40px`로 확대했다.
- 헤더 버전을 `Sprint 2.8`로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 맵 구성

- 트랙 시각 구역: `10`
- 분기 수: `3`
- 초반 분기 폭: `768px / 960px = 80%`
- 후반 분기 폭: `768px / 960px = 80%`
- 합류 폭: `300px / 960px = 31.25%`
- 결승 병목 폭: `196px / 960px = 20.4%`
- 섹션 수: `6`
- 범퍼: `11`
- 가이드 바: `26`
- 스피너: `3`
- 해머: `2`
- 이동 플랫폼: `1`
- 점프 구간: `3`
- C자 함정: `4`
- S자 통로: `2`
- 동적 장애물 수: `6`

### 섹션별 주요 장애물

- Section 1: `4개` - 범퍼 3, 스피너 1
- Section 2: `4개` - C자 함정 2, 해머 1, 범퍼 1
- Section 3: `3개` - 범퍼 2, 스피너 1
- Section 4: `4개` - 해머 1, 점프 구간 1묶음, C자 함정 1, 범퍼 1
- Section 5: `2개` - 범퍼 1, 이동 플랫폼 1
- Section 6: `4개` - C자 함정 1, 스피너 1, 점프 구간 1, 범퍼 1

### 유지한 값

- gravity: `0.080`
- damping: `0.994`
- rolling resistance: `0.992`
- camera follow: `0.18`
- camera deadzone: `120`
- physics ball radius: `16`
- visual ball size: `40px`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 확인
- 화면 버전 `Sprint 2.8` 반영 확인
- 물리값과 카메라값 유지 확인

### 남은 문제점

- 실제 브라우저 기준 평균 경기 시간은 추가 계측이 필요하다.
- 실제 플레이 기준 순위 변동 횟수는 추가 계측이 필요하다.
- 결승 병목에서 난전이 충분히 발생하는지 육안 확인이 필요하다.
- 기존 일부 한국어 UI 문자열은 콘솔 인코딩 이슈와 별개로 추후 정리가 필요하다.

## 디자인 소스 제작 - 예능 레이스 트랙 감성 강화

### 작업 내용

- `TRACK_ZONES`를 추가해 A / B / C, FAST / SAFE / CHAOS, MERGE, BOTTLENECK, SCRAMBLE 구간이 지도에 직접 보이도록 했다.
- FAST 루트는 빨간 계열, SAFE 루트는 파란 계열, CHAOS 루트는 보라 계열로 구분했다.
- 중앙 합류와 결승 병목 구간을 별도 트랙 구역으로 표시해 경쟁 구간처럼 보이게 했다.
- 장애물 색상, 두께, 그림자를 낮춰 참가자보다 먼저 튀지 않도록 했다.
- 참가자 공의 시각 크기를 `40px`에서 `44px`로 확대했다.
- 참가자 라벨을 소유자 이름만 표시하던 방식에서 번호가 포함된 `label` 표시로 바꿨다.
- 현재 선두 공에 노란 링과 강조 라벨을 적용했다.
- 우측 TOP 5 순위판에서 1위와 TOP3를 별도 스타일로 강조했다.
- 상단 HUD에 Leader, Move, Remaining, Map, Camera 정보를 추가했다.
- 물리값과 카메라값은 변경하지 않았다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### 디자인 요소

- 트랙 시각 구역: `10`
- 초반 분기: `A / B / C`
- 후반 재분기: `FAST / SAFE / CHAOS`
- 중앙 합류: `MERGE`
- 결승 병목: `BOTTLENECK`
- 결승 난전: `SCRAMBLE`
- visual ball size: `44px`
- physics ball radius: `16`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- `http://localhost:3000` 응답 확인
- 디자인 변경 핵심 코드 반영 확인
- 물리값과 카메라값 유지 확인

### 남은 문제점

- 실제 브라우저에서 참가자가 장애물보다 먼저 눈에 들어오는지 육안 확인이 필요하다.
- FAST / SAFE / CHAOS 루트 차이가 실제 관전 중 충분히 체감되는지 확인이 필요하다.
- 결승 병목에서 TOP 참가자들이 한 화면에 모이는지 확인이 필요하다.
- 기존 일부 한국어 UI 문자열은 추후 별도 정리 작업이 필요하다.

## Classic Map Blueprint v1 - 맵 전체 재구축

### 작업 내용

- 기존 Sprint 2.8 맵 구조를 유지하지 않고 `Classic Map Blueprint v1` 비율을 우선해 재구축했다.
- 전체 맵을 `START 10%`, `A/B/C 15%`, `MERGE 10%`, `S COURSE 20%`, `FAST/SAFE/CHAOS 20%`, `BOTTLENECK 10%`, `SCRAMBLE 10%`, `FINISH 5%` 흐름으로 재배치했다.
- 컨셉 이미지처럼 굵은 컬러 트랙 경로를 `TRACK_PATHS`로 추가했다.
- A/B/C와 FAST/SAFE/CHAOS 루트를 전체 폭의 80% 수준으로 명확히 분리했다.
- MERGE 구간을 하나로 합쳐지는 경쟁 구간으로 재설계했다.
- S COURSE는 좌우 이동이 4회 이상 발생하도록 하나의 긴 S 코스로 구성했다.
- BOTTLENECK은 TOP5가 한 화면에 모이도록 좁은 중앙 경로로 구성했다.
- SCRAMBLE은 결승 직전 최종 난전 구간으로 구성했다.
- Blueprint 금지 규칙에 맞춰 C자 함정을 제거했다.
- 동적 장애물은 스피너 1개만 유지했다.
- 해머와 이동 플랫폼은 이번 Blueprint 구현에서 제거했다.
- 장애물은 길을 막는 구조가 아니라 방향, 속도, 경로를 바꾸는 보조 요소로 축소했다.
- 화면 버전을 `Classic Blueprint v1`로 변경했다.

### 수정 파일

- `app/page.tsx`
- `다음_작업.md`
- `작업요약.md`
- `작업기록/2026-06-08.md`

### Blueprint 맵 구성

- 트랙 경로: `11`
- 트랙 시각 구역: `10`
- 분기 수: `3`
- 범퍼: `11`
- 스피너: `1`
- 해머: `0`
- 이동 플랫폼: `0`
- 점프 구간: `2`
- 가이드 바: `8`
- C자 함정: `0`
- S자 통로: `1`
- 바람 구간: `2`
- 동적 장애물 수: `1`

### 테스트 결과

- `npm run lint` 통과
- `npm run build` 통과
- 정상 이미지 파일 확인: `design/designclassic-map-concept-v1.png.png`
- 로컬 서버 화면 문자열 확인은 사용량 제한으로 실행하지 못했다.

### 남은 문제점

- 실제 브라우저에서 Blueprint 이미지처럼 레이스 트랙이 먼저 보이는지 확인이 필요하다.
- TOP5가 BOTTLENECK에서 한 화면에 모이는지 실제 플레이 확인이 필요하다.
- SCRAMBLE 구간에서 결승 직전 순위 변동이 충분한지 확인이 필요하다.
- 이미지 파일명이 `designclassic-map-concept-v1.png.png`로 되어 있어 추후 정리할 수 있다.
